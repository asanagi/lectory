# Class 2 · Lesson 2.6 — Cloudflare Worker config for SPA Web App.
# Copy to app/wrangler.toml in the product repo.
name = "app"
compatibility_date = "2024-09-23"
assets = { directory = "./dist", not_found_handling = "single-page-application" }
